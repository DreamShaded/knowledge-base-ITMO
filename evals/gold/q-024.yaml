id: q-024
category: multi_hop
query: "Как backpressure решает проблему перегрузки в реактивных системах?"

gold_sources: []  # populated during annotation phase

expected_facts:
  - "Backpressure — механизм управления потоком данных между компонентами"
  - "Позволяет consumer'у сигнализировать producer'у о своей пропускной способности"

min_hops: 2

thresholds:
  faithfulness: 0.85
  citation_recall: 0.70
  latency_p95_ms: 3000
